# PostgreSQL

PostgreSQL is the system of record for users, media metadata, feeds, live room state, notifications, reports, and admin audit logs.

Use migrations from the backend app once the ORM is finalized.
