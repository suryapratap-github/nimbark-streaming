# Infrastructure Notes

This folder is for deployment configuration and provider-specific notes.

- `cloudflare` - R2 buckets, CDN, domains, cache rules
- `livekit` - self-hosted LiveKit deployment notes
- `coturn` - TURN server deployment notes
- `postgres` - local and production database notes
